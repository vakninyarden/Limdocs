# Copy to .env.local and fill in your Cognito values (Vite exposes only VITE_* to the client).
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=
# API base (include /prod stage path).
VITE_API_URL=
