# Limdocs Progress Log

This file lives at `docs/progress.log.md` and is used for project progress tracking. It is tracked in Git (not listed in `.gitignore`).

## How To Use

- Append new updates at the end of the current date section.
- Use one line per milestone in this format:
  - `- <what changed> | files: <paths> | status: done/in-progress`
- Keep entries short and append-only.

## 2026-03-30

- Built the initial `LoginPage` flow with separate modes for login, signup, and account confirmation in a single screen. | files: `frontend/src/pages/LoginPage.jsx` | status: done
- Connected authentication flows to AWS Cognito via Amplify Auth (`signIn`, `signUp`, `confirmSignUp`, `getCurrentUser`, `signOut`) and integrated session checks/redirect behavior. | files: `frontend/src/pages/LoginPage.jsx`, `frontend/src/pages/HomePage.jsx` | status: done
- Added Hebrew RTL authentication UI styling and reusable page feedback/error patterns for auth actions. | files: `frontend/src/pages/LoginPage.css`, `frontend/src/pages/LoginPage.jsx` | status: done
- Upgraded signup validation messages to full Hebrew requirement lists (full name, email, username, password) including multi-line password requirements rendering support. | files: `frontend/src/pages/LoginPage.jsx`, `frontend/src/pages/LoginPage.css` | status: done
- Added login Cognito error mapping by field: unknown user/email under identifier, wrong password under password, unconfirmed user message with confirm-screen link support. | files: `frontend/src/pages/LoginPage.jsx` | status: done
- Implemented clear-on-change behavior for login and signup field errors, including removal of red error border on field edit. | files: `frontend/src/pages/LoginPage.jsx` | status: done
- Reworked HomePage into a static RTL dashboard shell with right-side permanent sidebar and modern purple/indigo visual style. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done
- Scoped HomePage down to basic shell only (no stats, no course grid, no search/notifications), preserving existing auth and logout behavior. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done
- Fixed sidebar placement and enforced right-side behavior in RTL layout based on UI feedback iterations. | files: `frontend/src/pages/HomePage.css` | status: done
- Added local progress log setup and ignore configuration for ongoing updates. | files: `.gitignore`, `progress.log.md` | status: done
- Added global Hebrew/English language switching with persisted preference and dynamic RTL/LTR direction handling across Login and Home pages. | files: `frontend/src/main.jsx`, `frontend/src/pages/LoginPage.jsx`, `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/LoginPage.css`, `frontend/src/pages/HomePage.css` | status: done
- Implemented shared language provider and translation dictionary, then renamed module naming from `i18n` to `language-control` (`LanguageControlProvider` + `languageContent`). | files: `frontend/src/language-control/LanguageControlProvider.jsx`, `frontend/src/language-control/languageContent.js`, `frontend/src/main.jsx` | status: done
- Localized Login/Home UI copy and auth feedback/errors to support both Hebrew and English while preserving existing Cognito auth logic. | files: `frontend/src/pages/LoginPage.jsx`, `frontend/src/pages/HomePage.jsx`, `frontend/src/language-control/languageContent.js` | status: done
- Updated Hebrew brand text so the Limdocs logo/name remains in English letters in login branding lines. | files: `frontend/src/language-control/languageContent.js` | status: done
- Added a styled Create Course modal popup from Home page with fields for course name, description, and visibility (private/public), including bilingual labels and placeholders. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css`, `frontend/src/language-control/languageContent.js` | status: done
- Split authentication into separate routes for login and register (`/login`, `/register`) while preserving existing auth flows and keeping `/` redirected to `/login`. | files: `frontend/src/App.jsx`, `frontend/src/pages/LoginPage.jsx` | status: done

## 2026-03-31

- Created DynamoDB `users` table to store Cognito user records synced after account confirmation (`user_id` as PK). | files: `AWS Console (DynamoDB)` | status: done
- Created DynamoDB `courses` table with composite key (`course_id` PK, `created_at` SK) and GSI on `owner_id` for owner-based course queries. | files: `AWS Console (DynamoDB)` | status: done
- Added Cognito Post Confirmation Lambda (Python 3.12) to sync `sub` + `email` into `users` table with ISO UTC `created_at`, wrapped in safe try/except and always returning event. | files: `AWS Console (Lambda)` | status: done
- Added Create Course Lambda (Python 3.12) behind API Gateway `POST /courses` to parse request body, extract Cognito `sub`, generate `course_id` UUID + `created_at`, and persist to `courses` table with logging/error handling and CORS response headers. | files: `AWS Console (Lambda)`, `AWS Console (API Gateway)` | status: done
- Integrated Home create-course modal submit flow with API Gateway call using Amplify session token and payload mapping (`course_name`, `description`, `is_public`) including in-flight state, inline error display, and disabled submit/cancel UI while request runs. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done
- Added frontend API endpoint configuration key `VITE_API_BASE_URL` for local/prod API routing and updated env template/local env. | files: `frontend/.env.example`, `frontend/.env.local` | status: done
- Updated create-course auth header token source from Cognito Access Token to ID Token to match API Gateway REST API Cognito Authorizer expectations and resolve `401 Unauthorized` during course creation. | files: `frontend/src/pages/HomePage.jsx` | status: done
- Added a new production-ready GET User Courses Lambda (Python 3.12) for `GET /users/{userId}/courses` with safe path/authorizer user extraction, env-driven DynamoDB query config, CORS proxy response helper, and robust error handling/logging. | files: `docs/lambdas/get_user_courses_lambda.py`, `docs/lambdas/get_user_courses_lambda.md` | status: done
- Implemented frontend "Get My Courses" service layer using `axios` with Bearer ID token auth and response normalization for both object (`{courses: [...]}`) and array payload formats. | files: `frontend/src/services/coursesService.js`, `frontend/package.json` | status: done
- Wired dashboard data loading to fetch current user's courses on auth-ready mount/user change using `useEffect`, with loading state, fetch error handling, and empty-state UX. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/language-control/languageContent.js` | status: done
- Added clickable My Courses card rendering in Home page content area (course-name focused), plus new section/grid/card styles using existing project CSS system. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done
- Updated dashboard greeting identity preference to show Cognito first name (`given_name`) instead of email fallback for a more user-friendly header greeting. | files: `frontend/src/pages/HomePage.jsx` | status: done
- Added post-create refresh behavior so after successful course creation the dashboard refetches courses immediately and shows the new course without manual reload. | files: `frontend/src/pages/HomePage.jsx` | status: done

## 2026-04-06

- Added backend IaC stack in-repo with SAM resources for Cognito User Pool + Client, DynamoDB `users`/`courses`, API Gateway (Cognito authorizer), and Lambda functions for create user/create course/get courses. | files: `backend/template.yaml`, `backend/src/create_user.py`, `backend/src/create_course.py`, `backend/src/get_courses.py` | status: done
- Updated user creation contract to store `username`, `first_name`, and `last_name` (instead of a single name field), with Cognito identity checks and DynamoDB persistence alignment. | files: `backend/src/create_user.py`, `frontend/src/pages/LoginPage.jsx` | status: done
- Implemented frontend user-sync flow to call `POST /users` after registration/confirmation using Cognito ID token, with pending/synced guards in storage to avoid duplicate inserts. | files: `frontend/src/pages/LoginPage.jsx` | status: done
- Added owner-scoped courses retrieval endpoint and authorization (`GET /users/{userId}/courses`) using `owner_courses_index` and strict `sub == path userId` checks. | files: `backend/src/get_courses.py`, `backend/template.yaml`, `frontend/src/services/coursesService.js`, `frontend/src/pages/HomePage.jsx` | status: done
- Standardized frontend API base URL resolution to support `VITE_API_URL` and `VITE_API_BASE_URL`, resolving stale/empty API base issues during login/home/courses flows. | files: `frontend/src/pages/LoginPage.jsx`, `frontend/src/pages/HomePage.jsx`, `frontend/src/services/coursesService.js`, `frontend/.env.example` | status: done
- Aligned create-course contract to use `course_name` and removed `title` dependency from active request/validation path. | files: `backend/src/create_course.py`, `frontend/src/pages/HomePage.jsx` | status: done
- Extended course records to include `owner_username` and `visibility` (`PUBLIC`/`PRIVATE`) based on Cognito claims and request visibility selection. | files: `backend/src/create_course.py` | status: done
- Cleaned all temporary debug instrumentation and session log artifacts after verification to return runtime code to production-clean state. | files: `frontend/src/pages/HomePage.jsx`, `.cursor/debug-216469.log` | status: done

## 2026-04-16

- Added course space route `/course/:courseId` with `CoursePage` and wired Home course cards to navigate with `courseName` in navigation state when `course_id` exists. | files: `frontend/src/App.jsx`, `frontend/src/pages/HomePage.jsx` | status: done
- Built course page shell: auth loading and guest redirect, purple banner with title from state and live materials count, upload toolbar, materials empty state (no mock cards), back-to-dashboard and language switcher with RTL/LTR layout. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css` | status: done
- Added `coursePage` i18n strings (Hebrew/English) for loading, navigation, materials, upload, and empty copy; removed obsolete keys when simplifying the page. | files: `frontend/src/language-control/languageContent.js` | status: done
- Implemented upload material modal: drag-and-drop and file picker, multi-file list with remove, Cancel/Escape/backdrop, then sequential upload with session handling and modal progress/success/error copy (bilingual). | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css`, `frontend/src/language-control/languageContent.js` | status: done
- Added SAM/API `GenerateUploadUrlFunction` for `POST /courses/{courseId}/upload-url` with DynamoDB document row, S3 presigned `put_object`, and CORS. | files: `backend/template.yaml`, `backend/src/generate_upload_url.py` | status: done
- Added frontend documents service `getUploadUrl` + `uploadFileToS3` and wired `CoursePage` to `useParams`, `fetchAuthSession`, per-file upload, and document refetch after success. | files: `frontend/src/services/documentsService.js`, `frontend/src/pages/CoursePage.jsx` | status: done
- Added `GetCourseDocumentsFunction` for `GET /courses/{courseId}/documents` querying `CourseIdIndex` GSI and returning `{ documents }` with CORS. | files: `backend/template.yaml`, `backend/src/get_course_documents.py` | status: done
- Wired `getCourseDocuments` in the app: two-column body with Materials inner nav, load on mount when authed, document cards with filename, created time, processing badge, and count from `documents.length`. | files: `frontend/src/services/documentsService.js`, `frontend/src/pages/CoursePage.jsx` | status: done
- Extended i18n for course inner nav, documents loading/error/empty/list ARIA, and uploaded status label. | files: `frontend/src/language-control/languageContent.js` | status: done
- Documented course documents architecture and UI notes in project docs. | files: `docs/course-documents-plan.md` | status: done
- Polished course page CSS: flatter layout on page surface, sidebar separator and soft active nav, premium document cards and status pill, full-width layout with `clamp` padding (removed fixed `960px` max width on key regions). | files: `frontend/src/pages/CoursePage.css` | status: done
- Verified production build after course page and documents work. | files: `frontend/` (npm run build) | status: done

## 2026-04-29

- Added backend delete endpoint infrastructure by introducing `DeleteCourseDocumentFunction` in SAM and wiring `DELETE /courses/{courseId}/documents/{documentId}` through `LimdocsApi` with default Cognito authorizer. | files: `backend/template.yaml` | status: done
- Implemented secure delete Lambda with auth/path validation, DynamoDB lookup, owner/course authorization checks, and strict S3-first-then-Dynamo deletion order inside try/except to prevent orphaned files. | files: `backend/src/delete_document.py` | status: done
- Added future cleanup reminder in delete Lambda for Textract pipeline outputs so generated processed artifacts can be deleted when that pipeline is added. | files: `backend/src/delete_document.py` | status: done
- Added frontend documents API client method `deleteDocument(courseId, documentId, idToken)` for authenticated DELETE requests with encoded params. | files: `frontend/src/services/documentsService.js` | status: done
- Upgraded course documents UI with per-card delete action, in-progress delete state, and optimistic local removal from `documents` via `.filter()` after successful deletion. | files: `frontend/src/pages/CoursePage.jsx` | status: done
- Replaced browser `window.confirm` with a styled in-app confirmation modal matching existing CoursePage modal patterns and premium visual language. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css` | status: done
- Added bilingual delete UX copy (confirm prompt/title/CTA, success, error, aria, deleting label) under `coursePage` content keys. | files: `frontend/src/language-control/languageContent.js` | status: done
- Validated new backend Lambda syntax and ran frontend lint; lint failure was due to pre-existing unrelated issues in other files, while changed delete-flow files passed without newly introduced lint problems. | files: `backend/src/delete_document.py`, `frontend/src/pages/CoursePage.jsx`, `frontend/src/services/documentsService.js`, `frontend/src/language-control/languageContent.js` | status: done
- Added backend cascading course-delete infrastructure in SAM with `DeleteCourseFunction` and authenticated route `DELETE /courses/{courseId}` via `LimdocsApi`. | files: `backend/template.yaml` | status: done
- Implemented `delete_course` Lambda with ownership checks, document query by `CourseIdIndex`, strict S3-first then DynamoDB document deletion, and final course deletion only after successful child cleanup. | files: `backend/src/delete_course.py` | status: done
- Added resilient failure handling in course cascade loop: if any document cleanup fails, Lambda returns `500` and does not delete the course record (retry-safe, avoids half-deleted DB state). | files: `backend/src/delete_course.py` | status: done
- Added future-proofing TODO in course delete flow to also remove Textract/processed artifacts once that pipeline bucket is introduced. | files: `backend/src/delete_course.py` | status: done
- Added frontend courses service method `deleteCourse(courseId, idToken)` for authenticated delete requests to the new course endpoint. | files: `frontend/src/services/coursesService.js` | status: done
- Upgraded Home page cards with subtle course delete action (trash button), click-isolation via `e.stopPropagation()`, styled confirm modal, loading/error handling, and instant local state removal using `.filter()`. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done
- Ensured Home last-course deletion gracefully falls back to existing empty-state rendering without layout breakage. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done
- Added Course page course-level delete CTA with styled confirmation modal, API call to `deleteCourse`, error/loading handling, and redirect to `/home` after successful deletion. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css` | status: done
- Added bilingual (HE/EN) localization keys for new delete-course UX under both `home` and `coursePage` sections (title, prompt, CTA, deleting, success/error, aria/session message). | files: `frontend/src/language-control/languageContent.js` | status: done
- Ran lint diagnostics on touched files; only existing cfn-lint runtime deprecation warnings (`python3.9`) in SAM remained, with no new blocking issues from the new feature code. | files: `backend/template.yaml`, `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/CoursePage.jsx`, `frontend/src/services/coursesService.js`, `frontend/src/language-control/languageContent.js` | status: done
- Added Textract pipeline IaC resources in SAM: `ProcessedOutputsBucket` with CORS, `ProcessDocumentFunction` S3 trigger from raw uploads, LabRole permission notes, and processed bucket output export. | files: `backend/template.yaml` | status: done
- Implemented `process_document` orchestration Lambda for S3 upload events with async Textract (`start_document_text_detection` + polling), paginated text aggregation via `NextToken`, processed text upload to `ProcessedOutputsBucket`, and DynamoDB status/key updates. | files: `backend/src/process_document.py` | status: done
- Added file-type safety gate in processing flow (`.pdf/.png/.jpeg/.jpg` only) to mark unsupported uploads as `FAILED` without calling Textract. | files: `backend/src/process_document.py` | status: done
- Hardened document update safety by fixing precise `document_id` parsing from upload key basename (`{document_id}_{filename}`), adding pre-update ID logging, and enforcing `ConditionExpression: attribute_exists(document_id)` on DynamoDB updates. | files: `backend/src/process_document.py` | status: done
- Increased processing Lambda timeout to 300 seconds to support async Textract polling on large PDFs and long academic documents. | files: `backend/template.yaml` | status: done
- Synced deletion cleanup pipeline so single-document and cascading course deletes now attempt removal from both raw and processed S3 buckets before DynamoDB record deletion. | files: `backend/src/delete_document.py`, `backend/src/delete_course.py` | status: done
- Added `PROCESSED_BUCKET` environment wiring for both delete Lambdas and introduced audit logs for bucket/key deletions with resilient error handling for missing S3 objects. | files: `backend/template.yaml`, `backend/src/delete_document.py`, `backend/src/delete_course.py` | status: done
- Re-validated updated backend handlers with Python compile checks and lint diagnostics; no new lint errors in changed Python files, with only existing SAM runtime deprecation warnings remaining. | files: `backend/src/process_document.py`, `backend/src/delete_document.py`, `backend/src/delete_course.py`, `backend/template.yaml` | status: done
- Implemented Phase 3 real-time frontend status polling in Course page: added final-status set (`EXTRACTED`, `FAILED`) and interval refetch while any document remains non-final, with cleanup on unmount to avoid leaks. | files: `frontend/src/pages/CoursePage.jsx` | status: done
- Added status badge mapping for pipeline states so document cards now show localized Processing/Ready/Error states (`UPLOADED` -> processing, `EXTRACTED` -> ready, `FAILED` -> error) and update live as polling refreshes state. | files: `frontend/src/pages/CoursePage.jsx` | status: done
- Added bilingual i18n keys for new processing statuses under `coursePage` (`statusProcessing`, `statusReady`, `statusFailed`) in Hebrew and English. | files: `frontend/src/language-control/languageContent.js` | status: done
- Verified deletion-sync implementation remained intact during Phase 3 execution (both raw + processed S3 cleanup paths and `PROCESSED_BUCKET` wiring for delete Lambdas). | files: `backend/template.yaml`, `backend/src/delete_document.py`, `backend/src/delete_course.py` | status: done
- Ran frontend diagnostics after Phase 3: changed files passed lints, while full `npm run lint` still reports pre-existing unrelated issues in `LanguageControlProvider.jsx` and `LoginPage.jsx`. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/language-control/languageContent.js`, `frontend/` | status: done
- Hardened quiz-generation infrastructure and secrets handling in SAM: added `OpenAIApiKey` parameter (`NoEcho`), wired `OPENAI_API_KEY` via `!Ref`, kept worker memory at `512`, and ensured explicit self-invoke Lambda permission for async worker enqueue. | files: `backend/template.yaml` | status: done
- Secured local deployment config by excluding `samconfig.toml` (root + backend path) from version control to prevent accidental secret leakage. | files: `.gitignore` | status: done
- Stabilized backend quiz generation flow with stronger async/worker guardrails and diagnostics: added duplicate-generation `409` protection, pre-invoke worker trace logs, full traceback logging on worker failures, and explicit `FAILED` status fallback behavior. | files: `backend/src/generate_questions.py` | status: done
- Improved AI response parsing resilience and observability by logging raw model output, handling markdown/prose-wrapped JSON extraction, warning on empty extracted document text, and logging truncated raw/cleaned payload details when no valid questions are produced. | files: `backend/src/generate_questions.py` | status: done
- Fixed question normalization schema mismatch so AI outputs using `answer` text are mapped into internal `correct_index`, while also supporting `topic`/`topics` variants and normalized/defaulted difficulty handling to reduce false rejections. | files: `backend/src/generate_questions.py` | status: done
- Updated quiz UX in Course page to allow selection/retry for `EXTRACTED`, `GENERATED`, and `FAILED` documents, preserve loading/disable lock semantics, and show clear background-start feedback after async generation requests. | files: `frontend/src/pages/CoursePage.jsx` | status: done
- Fixed question-set detail endpoint reliability by hardening `GET /courses/{courseId}/question-sets/{setId}` response serialization (including DynamoDB `Decimal` handling), normalizing returned question objects, and returning controlled `500` responses via `_response` instead of uncaught Lambda failures that surfaced as API Gateway `502`. | files: `backend/src/get_questions.py` | status: done
- Added guarded DynamoDB query error handling/logging for question-set list/detail paths (index query failures now log context and are caught by top-level handler). | files: `backend/src/get_questions.py` | status: done
- Fixed Course header regression (`Untitled Course` on Question Sets tab) by persisting course name in local page state, preserving router state during query-param tab sync, and adding deep-link/reload fallback metadata lookup by `courseId`. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/services/coursesService.js` | status: done
-   Re-validated both bugfix surfaces after implementation (`ReadLints` clean + Python compile check for updated backend handler). | files: `backend/src/get_questions.py`, `frontend/src/pages/CoursePage.jsx` | status: done

## 2026-05-13

- Learner Lab security/cost hardening: owner-only course access on upload URL, materials, quiz generation, question sets, and document delete via shared `course_access`; upload size gate (`MAX_UPLOAD_BYTES` + `file_size_bytes`); idempotent `process_document` (Dynamo claim UPLOADED→PROCESSING, S3 prefix `uploads/`, HeadObject size, safe logs); conditional READY→GENERATING claims before async quiz worker with worker `requestedBy` vs `owner_id` check; SAM reserved concurrency 2 on process + generate Lambdas; minimal CoursePage/documentsService wiring. | files: `backend/src/course_access.py`, `backend/src/limits.py`, `backend/src/generate_upload_url.py`, `backend/src/get_course_documents.py`, `backend/src/delete_document.py`, `backend/src/get_questions.py`, `backend/src/generate_questions.py`, `backend/src/process_document.py`, `backend/template.yaml`, `frontend/src/services/documentsService.js`, `frontend/src/pages/CoursePage.jsx`, `frontend/src/language-control/languageContent.js`, `docs/progress.log.md` | status: done
- Shipped Home dashboard "vibrant minimalist" shell: glass sidebar, ring/dot brand mark, nav (Dashboard / My Courses / Documents / Analytics) plus footer (language, Settings, Support) and logout; stub nav items remain presentational (no new routes or API). | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done
- Added sticky translucent top bar with welcome + subcopy, pill search (read-only), notifications control, and initials avatar; My Courses header row with subtitle and filter pill (presentational). | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done
- Reworked course cards to text-first layout (no thumbnails or subject line): title, document count + relative "updated" from `created_at` and optional count fields, silvery-blue gradient progress bar driven by optional progress fields when present. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done
- Restyled create-course grid tile (dashed icy tile, floating white circular plus, hint copy) and added fixed bottom-inline-end FAB pair (materials-style + help) for layout parity with the reference dashboard. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done
- Extended `home` i18n (Hebrew + English) for new dashboard copy: nav/footer labels, welcome strings, search/filter/notifications/avatar aria, course meta and progress labels, create-card hint, FAB labels. | files: `frontend/src/language-control/languageContent.js` | status: done
- Added Cursor Rules for Limdocs frontend work: design/UI/UX guidelines (bilingual RTL/LTR, visual language, accessibility, component states) and frontend engineering conventions scoped to `frontend/**/*.{js,jsx,css}`. | files: `.cursor/rules/limdocs-design-ui-ux.mdc`, `.cursor/rules/limdocs-frontend-engineering.mdc` | status: done
- Fixed SAM circular dependency for Textract pipeline by moving S3→Lambda invoke permission and bucket notification into explicit resources (`ProcessDocumentS3InvokePermission` + `DependsOn` chain) instead of implicit SAM event wiring on `ProcessDocumentFunction`. | files: `backend/template.yaml` | status: done
- Refreshed global design system via shared CSS variables in `index.css`: silvery-blue accent palette, motion/easing tokens, bento-style primary buttons, focus rings, and surface/typography neutrals; propagated restyle across Home, Course, and Login pages (theme helper scripts `_apply_theme_part1.py`, `_apply_theme_course.py`). | files: `frontend/src/index.css`, `frontend/src/pages/HomePage.css`, `frontend/src/pages/CoursePage.css`, `frontend/src/pages/LoginPage.css`, `frontend/index.html`, `frontend/public/icons.svg`, `_apply_theme_part1.py`, `_apply_theme_course.py` | status: done
- Extended `coursePage` i18n for quiz practice flow, submission feedback, and answer review labels (`submitQuiz*`, `correctAnswer`, etc.) in Hebrew and English. | files: `frontend/src/language-control/languageContent.js` | status: done

## 2026-05-24

- Fixed recursive Lambda invocation on quiz generation by splitting API (`limdocs-generate-questions`) and worker (`limdocs-generate-questions-worker`) functions, disabling worker async retries, marking quiz failures as `FAILED` without re-raise, and stopping `process_document` outer re-raise after terminal failure. | files: `backend/template.yaml`, `backend/src/generate_questions.py`, `backend/src/process_document.py`, `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css` | status: done
- Improved post-generate quiz UX on Course page: after `POST /generate-quiz` returns `202`, client polls document `processing_status` until selected docs leave `GENERATING` (success when `READY`, failure on `FAILED`/`ERROR` with `failure_reason`), refreshes question sets in parallel, and surfaces timeout when polling exceeds the configured window. | files: `frontend/src/pages/CoursePage.jsx` | status: done
- Added bilingual quiz-generation feedback strings for completion, timeout, generic failure, background-start success, materials refresh failure, and `GENERATING` status badge label. | files: `frontend/src/language-control/languageContent.js` | status: done
- Added `DeleteAttemptFunction` and authenticated route `DELETE /courses/{courseId}/attempts/{attemptId}`: course-owner gate via `course_access`, lookup attempt by user + course + attempt id, batch-delete `attempt_answers` rows then remove the attempt summary row. | files: `backend/src/delete_attempt.py`, `backend/template.yaml` | status: done
- Upgraded `delete_course` cascade to remove full course footprint before deleting the course record: all documents (raw + processed S3 + DynamoDB), all question sets and their questions, and all user attempts plus attempt-answer rows for that course; response includes deleted counts. | files: `backend/src/delete_course.py`, `backend/template.yaml` | status: done
- Wired Attempts tab delete flow: per-attempt trash control, styled confirm modal, `deleteAttempt` API client, optimistic list removal, and Hebrew/English copy for delete-attempt UX. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css`, `frontend/src/services/coursesService.js`, `frontend/src/language-control/languageContent.js` | status: done
- Implemented bilingual sub-topic extraction (Step 1): shared `openai_helpers.py` (JSON utils, warm OpenAI client, `FALLBACK_TOPICS`), refactored quiz worker to reuse helpers with 12k char budget unchanged, `ProcessDocumentFunction` calls `gpt-4.1-mini` after Textract (100k char budget, 3–6 `{he,en}` topics or General fallback, always `READY` on AI failure), legacy docs get injected `topics` on `GET /courses/{courseId}/documents`, template env + 512 MB memory on process Lambda. | files: `backend/src/openai_helpers.py`, `backend/src/generate_questions.py`, `backend/src/process_document.py`, `backend/src/get_course_documents.py`, `backend/template.yaml` | status: done (deploy pending: SAM CLI + AWS creds not available in agent environment)
- Implemented quiz topic constraints (Step 2): three-layer enforcement — OpenAI `json_schema` enum on `topics`, dynamic strict system prompt with allowlist, post-validation via casefold canonical lookup with `["General"]` fallback (never discard for topic mismatch); worker loads topics in single sequential `get_item` loop with `ensure_document_topics` + `dedupe_topics_by_en`; no SAM/template changes. | files: `backend/src/openai_helpers.py`, `backend/src/generate_questions.py` | status: done (deploy + manual verify pending)
- Implemented performance matrix on quiz submission (Step 3): added DynamoDB `user_progress` table (PK `user_name`, SK `course_id`) with nested `matrix` map; `submit_attempt` aggregates per-topic/per-difficulty integer deltas in memory via `defaultdict`, then flushes with atomic `ADD` updates using exception-driven two-step upsert (optimistic increment + init fallback on `ValidationException`); strict mode returns 500 if matrix flush fails after attempt persistence. | files: `backend/template.yaml`, `backend/src/submit_attempt.py` | status: done (deploy + manual verify pending)

## 2026-05-23

- Added DynamoDB `attempts` (PK `user_name`, SK `submitted_at`) and `attempt_answers` (PK `attempt_id`, SK `question_id`) tables in SAM for persisted quiz-attempt records. | files: `backend/template.yaml` | status: done
- Implemented `SubmitAttemptFunction` for `POST /courses/{courseId}/question-sets/{setId}/attempts`: owner gate via `course_access`, validates `time_spent_seconds` + `answers` map, grades against question set, writes attempt summary + per-question answer rows (with explanations), returns `{ attempt_id, score }`. | files: `backend/src/submit_attempt.py`, `backend/template.yaml` | status: done
- Built Course page practice-quiz UX: start attempt from a question set, select answers per question, submit to API with elapsed time, switch to results mode with localized correct/incorrect answer styling and score feedback. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css`, `frontend/src/services/coursesService.js`, `frontend/src/language-control/languageContent.js` | status: done
- Added `GetCourseAttemptsFunction` for `GET /courses/{courseId}/attempts` listing the authenticated user's submitted attempts for a course (score, time spent, question set id, submitted_at), with owner authorization. | files: `backend/src/get_course_attempts.py`, `backend/template.yaml` | status: done
- Added `GetAttemptAnswersFunction` for `GET /courses/{courseId}/attempts/{attemptId}/answers` returning stored user answers for a past attempt after ownership/attempt lookup checks. | files: `backend/src/get_attempt_answers.py`, `backend/template.yaml` | status: done
- Added Course page Attempts tab: loads attempt history on tab select, renders clickable attempt cards (date, score, time, question set), and opens read-only review mode by fetching stored answers + question set detail. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css`, `frontend/src/services/coursesService.js`, `frontend/src/language-control/languageContent.js` | status: done
- Extended `coursesService` with `submitAttempt`, `getCourseAttempts`, and `getAttemptAnswers` API client methods using Bearer ID token auth. | files: `frontend/src/services/coursesService.js` | status: done

## 2026-05-24

- Added Step 4 analytics read path: `GetUserProgressFunction` for `GET /courses/{courseId}/progress` returns `{ matrix }` from `user_progress` (empty `{}` when no item), with Cognito auth and `require_course_owner`. | files: `backend/src/get_user_progress.py`, `backend/template.yaml` | status: done (deploy + LabRole GetItem verify pending)
- Course page Step 4 UI: mount-time materials prefetch for topic catalog; document topic chips on Materials tab; fourth inner tab “Analyzed Weaknesses” with mastery cards, CSS-tier progress bars, `useMemo` catalog/rows, and full `he`/`en` i18n. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css`, `frontend/src/services/coursesService.js`, `frontend/src/language-control/languageContent.js` | status: done

## 2026-06-03

- Added shared weighted topic-scoring engine: difficulty weights (Easy=1, Medium=2, Hard=3), per-topic weighted percent score, weak/medium/strong status bands (≤59 / ≤79 / 80+), per-difficulty breakdown, legacy `General`→`Uncategorized` merge, and weakest-first sort. | files: `backend/src/topic_scoring.py`, `frontend/src/utils/topicScoring.js` | status: done
- Upgraded `GET /courses/{courseId}/progress` to return `{ course_id, matrix, topics }` with server-side `compute_topic_scores(matrix)` so clients can render analytics without reimplementing scoring. | files: `backend/src/get_user_progress.py` | status: done
- Aligned quiz submission progress aggregation with scoring labels by using `Uncategorized` as the matrix fallback topic key (replacing `General`) so attempt deltas match weakness analytics. | files: `backend/src/submit_attempt.py` | status: done
- Added frontend `getCourseProgress` client and `resolveProgressTopics` helper: prefers API `topics` when present, otherwise derives scores from `matrix` for backward compatibility. | files: `frontend/src/services/coursesService.js`, `frontend/src/utils/topicScoring.js` | status: done
- Extended Analyzed Weaknesses tab: lazy-load progress on tab select, vertical `TopicScoreChart` (color-coded bars + legend + score explainer), per-topic cards with weighted score, status chip, and Easy/Medium/Hard breakdown rows tied to bilingual topic catalog labels. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/components/TopicScoreChart.jsx`, `frontend/src/pages/CoursePage.css` | status: done
- Added bilingual i18n for topic score chart and status UX (`weaknessScoreExplainer`, `weaknessStatusWeak|Medium|Strong`, chart ARIA/legend/bar labels, `weaknessTopicScoreLabel`). | files: `frontend/src/language-control/languageContent.js` | status: done
- Added unit tests for topic scoring edge cases: weighted mixes, empty/zero cells, General merge, status thresholds, sort order, and difficulty breakdown percents. | files: `backend/tests/test_topic_scoring.py` | status: done
- Documented full Lambda trigger/data-flow map (API Gateway, S3 Textract pipeline, async quiz worker, DynamoDB touchpoints) with Mermaid diagram for onboarding and ops reference. | files: `docs/lambda-functions-flow.md` | status: done
- Extracted shared `progress_matrix` module for attempt→matrix deltas: build/serialize/merge/negate, atomic `ADD` upserts, negative apply on delete, and full-matrix rebuild from remaining attempts when counters drift (`MatrixDriftError`). | files: `backend/src/progress_matrix.py`, `backend/src/submit_attempt.py` | status: done
- Persisted per-attempt `matrix_deltas` on submit so delete paths can reverse exact contribution without re-querying all answers when possible. | files: `backend/src/submit_attempt.py` | status: done
- Upgraded `delete_attempt` to subtract stored attempt deltas from `user_progress` (with rebuild fallback on drift) after removing attempt + answer rows. | files: `backend/src/delete_attempt.py` | status: done
- Extended `delete_course` cascade to remove the owner's `user_progress` row for that course after attempts/question sets/documents cleanup. | files: `backend/src/delete_course.py`, `backend/template.yaml` | status: done
- Added unit tests for progress matrix delta math, serialization, merge/negate, and rebuild edge cases. | files: `backend/tests/test_progress_matrix.py` | status: done
- Enhanced quiz generation API/worker: configurable `requested_question_count` (5/10/15/20), `quiz_language` (`he`/`en`) with strict language blocks, source-fidelity system prompt, OpenAI `json_schema` min/max item counts, and persisted generation metadata on question sets. | files: `backend/src/generate_questions.py`, `backend/src/get_questions.py` | status: done
- Added Course page quiz-generation options modal: question-count radios, Hebrew/English content language, wired `POST /generate-quiz` body (`requested_question_count`, `quiz_language`), plus question-set cards showing language badge and requested vs actual counts. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css`, `frontend/src/services/documentsService.js`, `frontend/src/language-control/languageContent.js` | status: done
- Implemented weakness-focused quiz generation: API accepts `focus_weak_topics`, worker loads `user_progress`, picks up to five weakest canonical topics via `select_prioritized_weak_topics`, injects WEAK-TOPIC PRIORITY prompt block, and tags sets with `generation_mode` `WEAKNESS_FOCUSED` + `focused_topics` metadata. | files: `backend/src/generate_questions.py`, `backend/src/topic_scoring.py`, `backend/template.yaml` | status: done
- Added Course page "focus on my weak topics" checkbox (quiz modal + persisted preference) and `focus_weak_topics` client payload; extended topic-scoring and generate-questions unit tests for weak-focus parsing/resolver/prompt behavior. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css`, `frontend/src/services/documentsService.js`, `backend/tests/test_generate_questions.py`, `backend/tests/test_topic_scoring.py` | status: done
- Wired Home dashboard course cards to live stats: per-course parallel fetch of documents + progress, `courseCardStats` helper for document count, last-updated ISO (course or latest doc), and progress bar percent from averaged topic mastery scores. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/utils/courseCardStats.js`, `frontend/src/services/coursesService.js` | status: done
- Added Home loading/pending UX for course meta and progress bar (`courseMetaDocsLoading`, pending meta styling, `aria-busy`) while stats requests are in flight. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css`, `frontend/src/language-control/languageContent.js` | status: done

## 2026-08-16

- Replaced the Home sidebar Limdocs headline/circle mark with the final logo image (`logo_limdocs_final.png`) and sized it to the brand slot. | files: `frontend/src/assets/logo_limdocs_final.png`, `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css` | status: done

## 2026-08-24

- Rewrote README to match the live system: Cognito auth, Textract + OpenAI pipeline, quiz generation (count/language/weak-focus), attempts, weakness analytics, owner-only course APIs, and AWS resource table. | files: `README.md` | status: done
- Removed outdated course-documents plan doc after it no longer matched the implemented pipeline. | files: `docs/course-documents-plan.md` | status: done

## 2026-08-30

- Replaced the purple-lightning SVG favicon with the Limdocs symbol PNG and set the browser tab title to `Limdocs`. | files: `frontend/index.html`, `frontend/public/favicon.png` | status: done
- Added the Limdocs symbol image on login and register headers so auth screens match dashboard branding. | files: `frontend/src/pages/LoginPage.jsx`, `frontend/src/pages/LoginPage.css`, `frontend/src/assets/limdocs-symbol.png` | status: done
- Added `visibility_courses_index` GSI on `courses` (`visibility` HASH + `created_at` RANGE, INCLUDE `course_name`/`owner_id`/`updated_at`) and `GET /courses/public` Lambda with paginated public-course query, sequential per-course document aggregation, server-derived `is_owner`, and whitelist sanitization. | files: `backend/template.yaml`, `backend/src/get_public_courses.py`, `backend/tests/test_get_public_courses.py`, `backend/tests/test_get_courses.py` | status: done
- Split Home dashboard into **My Courses** / **Explore Courses** tabs: Explore refetches on every tab switch, owned public courses reuse full owner cards via `OwnerCourseCard`, foreign public courses render as static metadata-only cards; added `getPublicCourses` client, tab CSS, and bilingual i18n. | files: `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css`, `frontend/src/services/coursesService.js`, `frontend/src/language-control/languageContent.js` | status: done
- Added accessible courses tablist keyboard navigation (RTL-aware arrows, Home/End) and kept Explore in sync when an owned public course is deleted from My Courses. | files: `frontend/src/pages/HomePage.jsx` | status: done
- Documented `GET /courses/public` in README (API table, 17 Lambdas, reserved `/courses/public` path) and added `get_public_courses` to the Lambda flow diagram. | files: `README.md`, `docs/lambda-functions-flow.md` | status: done
- Added backend unit tests for public-course auth/sanitization/pagination plus `get_courses` owner-scope regression; local `pytest tests/` 59 passed and frontend production build succeeded. | files: `backend/tests/test_get_public_courses.py`, `backend/tests/test_get_courses.py` | status: done
- Pre-deployment read-only inspection for courses missing `visibility` or `created_at`: not run in this session (requires live AWS credentials against the `courses` table before stage-1 deploy). | files: `docs/progress.log.md` | status: pending
- Added owner-only `PATCH /courses/{courseId}/visibility` Lambda (`update_course_visibility`) with PUBLIC/PRIVATE enum, idempotent same-value 200, `updated_at` on change, `created_at` GSI backfill when missing, and CORS PATCH on the REST API. | files: `backend/src/update_course_visibility.py`, `backend/template.yaml`, `backend/tests/test_update_course_visibility.py` | status: done
- Course page always loads persisted visibility from `GET /users/{userId}/courses`; owners can toggle Public/Private (confirm only when going public) via PATCH, with in-flight disable and previous value kept on failure. | files: `frontend/src/pages/CoursePage.jsx`, `frontend/src/pages/CoursePage.css`, `frontend/src/services/coursesService.js`, `frontend/src/utils/courseVisibility.js` | status: done
- Added a shared Public/Private pill (lock/globe + localized text) on My Courses, owned Explore, and foreign Explore cards. | files: `frontend/src/components/CourseVisibilityBadge.jsx`, `frontend/src/components/CourseVisibilityBadge.css`, `frontend/src/pages/HomePage.jsx`, `frontend/src/pages/HomePage.css`, `frontend/src/language-control/languageContent.js` | status: done
- Documented PATCH visibility in README (API table, 18 Lambdas) and added `update_course_visibility` to the Lambda flow diagram. | files: `README.md`, `docs/lambda-functions-flow.md` | status: done
- Backend `pytest tests/` 78 passed after adding `test_update_course_visibility`; frontend production build succeeded. SAM CLI is not installed on this machine so `sam validate` / `sam build` were not run. | files: `backend/tests/test_update_course_visibility.py` | status: done
